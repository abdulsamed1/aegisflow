{
  "name": "aegisflow",
  "version": "1.0.0",
  "description": "BMEIA Austria Visa Appointment Automation Platform",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "d1:init": "wrangler d1 execute opran-booking-db --file=db/schema.sql"
  },
  "dependencies": {
    "@cloudflare/playwright": "^1.3.0",
    "tesseract.js": "^5.1.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240815.0",
    "playwright-core": "^1.62.1",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.72.0"
  }
}