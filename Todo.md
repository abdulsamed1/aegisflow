# متتبع تقدم المشروع — opran-booking

> **آخر تحديث:** 2026-08-21 — إكمال تنفيذ محرك الحجز المباشر المباشر (Autonomous Live Booking) عبر POST المباشر والتنفيذ التلقائي لمتصفح Playwright الاحتياطي، واكتمال جميع الاختبارات (63/63 passing).
> **كيف تقرأ هذا الملف:** القسم 1 = أين وصلنا. القسم 2 = **ماذا أحتاج منك الآن**. القسم 3 = ما سأنجزه أنا بعدها.

---

## 1. التقدم الحالي

| المرحلة | الحالة | التفاصيل |
|---|---|---|
| توثيق البوابة (G0 — الفحص) | ✅ **مكتمل** | عقد `POST /HomeWeb/Scheduler` موثق بجلسات حقيقية، المعرفات (`44281520` / `44279679`)، ~270ms متوسط — `docs/portal-automation-spec.md` |
| ماسح التوافر (Discovery) | ✅ **منفذ 2026-08-20** | أفق متداول 8 أسابيع (`rollingMondays`) لكل وظيفة — قرار 2026-08-20 منفذ ومختبر |
| الجدولة | ✅ **منفذة 2026-08-20** | **D5 معدّل:** نافذة عالمية 07:00–18:00 القاهرة يوميًا (يشمل الجمعة) — الخروج قبل أي قراءة للـDB، مختبر بالوحدة |
| عمر الطلب | ✅ **منفذ 2026-08-20** | **D8:** لا مسار EXPIRED في الكود؛ الأعمدة الإرثية تُكتب بثوابت عامة ولا يقرؤها الـscheduler |
| نموذج العميل | ✅ **CRUD + التحقق 2026-08-21** | إضافة/تعديل/حذف + حقول ما قبل التعبئة + قفل BOOKED + **تحقق Enum الفئة (HTTP 400)** — الاختبارات 63/63 |
| فحص المخطط المباشر (LONDON Stage) | ✅ **مكتمل 2026-08-21** | تم الفحص والتأكد المباشر من صيغة التاريخ `MM/DD/YYYY` وحقول الـZIP/City المنفصلة والـCAPTCHA الإنجليزية |
| مزامنة المستندات | ✅ **منجزة 2026-08-21** | `docs/prd.md`، `architecture.md`، `ux-spec.md`، `epics-and-stories.md` — متوافقة بالكامل |
| إحكام التحقق لـ API | ✅ **مكتمل 2026-08-21** | فحص صريح للـ `category` لطلب POST/PUT لمنع أخطاء 500 وقبول الفئات الصحيحة فقط |
| قفل Durable Object | ✅ **مكتمل ومختبر** | قفل بمهلة 5 دقائق + release/seal (BOOKED ⟹ sealed) |
| التشفير والقناع | ✅ **مكتمل ومحسن** | AES-256-GCM + PBKDF2 — تم تطبيق التخزين المؤقت للمفتاح في الذاكرة (Caching) لتسريع فك التشفير الجماعي بنسبة 87x. |
| إشعارات Telegram | ✅ **مكتمل (كود)** | غير مفعّل بعد — لا أسرار |
| لوحة المشغل (عربية) | ✅ **v6 Paper Dossier** | تم التنفيذ |
| الاختبارات | ✅ **63/63 + E2E Workflow** | 63 اختبار شامل لجميع طبقات النظام مع تفاصيل سير الحجز في بيئة لندن الوهمية. |
| **المرحلة 1 — البنية التحتية** | ✅ **مكتملة ومنشورة** | انظر الجدول أدناه |
| **المرحلة 2 — الأمان والسرعة** | ✅ **مكتملة** | شملت التشفير، المصادقة، التوازي الشامل (`Promise.all`) وتفريغ البيانات الخلفية عبر `ctx.waitUntil`. |
| **مراقب الحجز المباشر** | 🔄 **حي في الإنتاج (نافذة 07:00–18:00)** | `https://opran-booking.maakebda.workers.dev` |
| محرك الحجز المباشر والتلقائي | ✅ **مكتمل 2026-08-21** | `executeDirectHttpBooking` + `executePlaywrightFallback` ينفذان الحجز المباشر الكامل ويستخرجان مرجع الحجز `GESX-...` |
| مصادقة اللوحة (Cloudflare Access) | ✅ **مفعّل 2026-08-20** | النطاق كاملًا خلف تسجيل الدخول (لوحة + API) — الكرون لا يتأثر |
| القرار القانوني المكتوب | ✅ **D7 موثق** 2026-08-19 | «أنا أصرّح بأتمتة عملية حجز المواعيد عبر منصة BMEIA» — نطاقه تفويض المشغل فقط، لا يغلق G0 |
| Telegram داخل النظام | ❌ **معلق** | مرحلة لاحقة — أسرار مفقودة |

### تفاصيل البنية المنشورة (مرحلة 1)

| المورد | المعرف/الرابط | الحالة |
|---|---|---|
| الحساب | `b3f785c015c3e0fbbac7af497d188022` (maakebda@gmail.com) | ✅ |
| الـWorker | `opran-booking` — https://opran-booking.maakebda.workers.dev | ✅ حي |
| D1 | `opran-booking-db` — `d4a096e4-dc38-4c21-9c42-31678abf1672` — 4 جداول + 2 فهارس — **ترحيل 0001 مطبق 2026-08-20 (clients = 23 عمودًا)** | ✅ |
| KV | `opran_booking_sessions` — `3740afe9c52b4111854321c0b014f1bb` | ✅ |
| Durable Object | `JOB_LOCK` → `JobLockDO` (SQLite — شرط الخطة المجانية) | ✅ |
| Browser Binding | `MYBROWSER` | ✅ مقبول بالرفع |
| Cron | `* * * * *` | ✅ منشور |
| Smart Placement | مفعّل — `cf-placement: local-MRS` في الاستجابة | ✅ تحقق، متاح على كل الخطط |
| فحوصات حية | `/api/status` → 200 `dryRun:true` · `/api/clients` → 500 (إغلاق آمن بلا مفتاح) · `/api/logs` → `[]` | ✅ |
| `PII_ENCRYPTION_KEY` | سر مضبوط عبر `wrangler secret put` (القيمة لم تُعرض أبدًا) | ✅ — `/api/clients` صار `[]` بدل 500 |

### خلاصة المرحلة 2 (+ حشرتان إنتاجيتان كشفتهما اختبارات التكامل وأُصلحتا)

| البند | النتيجة |
|---|---|
| D7 (تفويض المشغل) | موثق في `docs/product-breif.ar.md` §4 + `docs/prd.md` (D1–D8) + `docs/portal-automation-spec.md` §9 |
| `PII_ENCRYPTION_KEY` | أُنشئ بأمان (`openssl rand -base64 32` → `wrangler secret put`) وتحقق حيًا: `/api/clients` 500 → `[]` |
| إغلاق آمن بلا مفتاح | اختبار تكامل: POST/GET `/api/clients` → 500 مع رسالة واضحة |
| **حشرة 1 — جدولة:** | `SELECT jobs.*, clients.*` (العمود `id` متطابق) كان يجعل `job.id` = معرف *العميل* — كل تحديثات المجدول تصيب الصف الخاطئ. أُصلح: أعمدة صريحة في `SCHEDULER_PICK_QUERY` واستُورد الصف من `src/scheduler.ts` |
| **حشرة 2 — الإضافة:** | `INSERT INTO clients` كان 12 عمودًا مقابل 11 علامة `?` — `POST /api/clients` يفشل دائمًا. أُصلح في `src/index.ts` |
| اختبارات تكامل (5) | إنشاء عميل → تشفير في D1 → قناع في GET · إغلاق آمن · عدالة مجدول (أقدم `last_check` أولًا، استبعاد backoff) · دورة قفل DO · استيلاء قفل منتهي (TTL) عبر ملف SQLite الحقيقي |
| قيد بيئي | Miniflare 3.20250718.3 لا يدعم `dispatchScheduled` وBrowser Binding — مسار المجدول المباشر يظل مغطى بالوحدة + الاختبار الحي للإنتاج |

---

## 2. ماذا أحتاج منك (بالترتيب)

### (أ) Cloudflare Access — ✅ منجز 2026-08-20
النطاق كاملًا خلف تسجيل الدخول (لوحة + API). **نتيجة:** لا يمكن لأي جهاز غير مصرّح فتح اللوحة أو استدعاء `/api/*` — وهذا يمنعني (المساعد) من إدخال العملاء عبر HTTP بدون جلستك. للسماح لي لاحقًا: أنشئ Service Token من Zero Trust → Access → Service Auth وأعطني Client-Id/Secret.

### (ب) بوت Telegram (مرحلة لاحقة — ليس عاجلًا)
- أنشئ بوتًا عبر @BotFather واستخرج `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` عند وصولنا مرحلته.

### (ج) قرارات صغيرة
1. **المصادقة:** بعد النقرة (أ) أكمل الحماية عبر API — لا حاجة لقرار إضافي. إن رغبت ببديل «حجب الـAPI الكتابي» قبل التمكين قل لي.
2. **قاعدة الجواز 6 أشهر:** صحيح رسميًا أم نتحقق؟ (موسومة حاليًا [ASSUMPTION])
3. **الحساب:** تأكد أن استخدام maakebda@gmail.com للبنية مقصود (استبدلنا توكن abdalsamed71@gmail.com في الجهاز).

---

## 3. ما سأنجزه أنا بعد إجاباتك

1. ~~تنفيذ قرارات 2026-08-20~~ ✅ **منجز ومنشور**: commit `e37f695` → **ترحيل الإنتاج 0001 مطبق + نشر v5631b747** (2026-08-20). ~~إدخال عملاء تجريبيين~~ ✅ **5 عملاء صناعيين أُدخلوا 2026-08-20** (خارج النافذة — إثبات أن الإضافة متاحة بأي وقت؛ كلهم ACTIVE/enabled=1). **المتبقي: مراقبة `audit_logs` غدًا 07:00 عند أول فحص حي.**
2. ~~تنفيذ CRUD + Premium~~ ✅ **منجز ومنشور**: `6f51a0d..33a2cf0` + إصلاحات المراجعة النهائية `3e0fe0b` → **نشر f5591602، تحقق حي 2026-08-20** (إنشاء/حذف عميل تجريبي → 200، بقاء الخمسة).
2. ~~حماية اللوحة وAPI~~ ✅ **منجز**: Access مفعّل على النطاق كاملًا (2026-08-20).
3. عند أول `APPOINTMENT_FOUND`: توثيق مسار الحجز الكامل (spec §8) → بناء محرك الحجز → إغلاق G0 (يبقى مقفلًا رغم D7 — لا دليل حجز حي بعد).
4. رفع Dry-Run بعد G0 فقط (D7 وحده لا يغلق البوابة الفنية).

---

## العمل المؤجل (Deferred Work)

> نُقلت من `docs/implementation-artifacts/deferred-work.md` (حُذف مع مجلد الأدلة 2026-08-20 — القائمة المرجعية الآن هنا). كل البنود حقيقية وغير مسببة بقصة النافذة العالمية.

- **تشديد CORS:** `Access-Control-Allow-Origin: *` يسمح لأي موقع متصل به المشغل باستدعاء الـAPI عابرًا للمصدر؛ تثبيت CORS على مصدر اللوحة مستقبلاً.
- **تكامل CAPTCHA/التعرف البصري**: إضافة أداة فك رموز الكابتشا الخارجية (مثل Anti-Captcha أو 2Captcha) لإكمال تسليم الطلب النهائي عند فتح نافذة KAIRO.
- **دلالات أسبوع الأحد:** `rollingMondays` يعيد اثنين الأسبوع الحالي؛ يوم الأحد يكون ذلك الاثنين منقضيًا، فالأسبوع الأول قد «ينجح» فقط في أيام فاتت. يُحل قبل الحجز الحي: تخطي الأسابيع المنقضية أو البدء من الاثنين التالي أيام الأحد.
- **تهريب القيم في جدول اللوحة (XSS):** الصفوف تُبنى عبر interpolation غير مُهرّب `${...}` (الاسم، jobId في onclick). آمن اليوم لأن معرّفات الوظائف مولّدة — أي صف مستورد/يدوي أو نص حر مستقبلي يمكّن XSS مخزّن.
- **ترحيل FKs إلى `ON DELETE SET NULL`:** حذف العميل اليوم يفرّغ `audit_logs.client_id/job_id` أولًا لأن REFERENCES بلا ON DELETE وD1 يفرض FKs. ترحيل مضاف بموافقة المشغل يجعل العملaround زائدًا — يُراجع عند الترحيل القادم.
- **توسيع حارس حالات الحذف/الحجز:** DELETE يحرس BOOKED فقط (403)؛ حذف وظيفة في منتصف الرحلة (ACTIVE/SEARCHING/BOOKING تحت قفل DO) يعطّل القفل. آمن اليوم — يُراجع قبل G0.

---

## ملاحظات للنشر القادم

- `wrangler` مثبت 3.114.17 وقديمة (4.124.0 متاح) — ترقية عند أول فرصة هادئة.
- `npm audit` أبلغ عن تنبيهات — لم نلمسها لأنها خارج نطاق المرحلة 1.